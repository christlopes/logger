# Railway deploy config: create/update DB tables before starting the app.
# Uses prisma db push (no migration history). For migrations, use prisma migrate deploy instead.

[build]
builder = "nixpacks"

[deploy]
# Tables are created by the start script (prisma db push runs inside "npm start")
# No custom startCommand needed - default "npm start" runs the script above
